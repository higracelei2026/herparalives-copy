# 环境与上线清单

| 环境 | 入口 | 数据 | AI | 用途 |
|---|---|---|---|---|
| development | 本地 | 浏览器 / 模拟 | 安全模板 | 开发 |
| staging | 预览分支 | CloudBase 测试环境 | 百炼真实调用 | 内容与压力验收 |
| demo | 临时域名 | 独立活动环境 | 预设零调用，自定义可降级 | Demo Day |
| production | 备案自定义域名 | CloudBase 生产环境 | 百炼真实调用 | 正式上线 |

## Demo Day

- 冻结并记录 `/api/presets` 的版本号。
- 断开模型密钥，完整玩通指定预设角色。
- 以 50 个并发会话验证首页、角色大厅和预设剧情。
- 自定义文本并发上限 20、图片并发上限 5；超过时显示队列状态。
- 准备完整录屏；模型不可用时隐藏自定义入口。

## 正式切换

- CloudBase 必须选择上海地域，以启用手机号验证码登录。
- 数据库、对象存储、认证环境与测试环境隔离。
- EdgeOne 中只配置服务端密钥；浏览器可见环境变量不得包含凭证。
- 自定义域名完成 ICP 备案后再绑定生产环境。
